export interface ISiteMeta {
  pageTitle: string;
  pageDescription?: string;
  pageURL: string;
}

export interface IConfig {
  title: string;
  description: string;
  keywords: string;
  URL: string;
  image: string;
  author: string;
  version: string;
}
